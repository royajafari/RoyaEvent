fastapi==0.141.1
uvicorn[standard]==0.52.1
gunicorn==23.0.0
pydantic==2.13.4
pydantic-settings==2.15.0
SQLAlchemy==2.0.36
alembic==1.19.1
redis==8.1.0
pymongo==4.10.1
minio==7.2.20
python-jose[cryptography]==3.5.0
slowapi==0.1.10
apscheduler==3.11.0
jinja2==3.1.5
httpx==0.28.1
python-multipart==0.0.20
Pillow==12.3.0

# اعلان‌ها (فاز ۶) — تبدیل UTC به تقویم/ساعت جلالی تهران برای متن پیامک/ایمیل
# (نمایش شمسی در فرانت با Intl بومی مرورگر انجام می‌شه، ولی اینجا سرور خودش
# باید تبدیل کنه، مرورگری در کار نیست). tzdata لازمه چون ویندوز و ایمیج‌های
# داکر slim دیتابیس IANA سیستمی رو با خودشون ندارن، zoneinfo بدونش fail می‌ده.
jdatetime==6.1.0
tzdata==2026.3

# جستجوی محتوایی (فاز ۴) — sentence-transformers خودش torch رو transitive
# می‌کشه؛ حتماً قبل از نصب این دو خط، torch نسخه‌ی CPU-only رو جدا نصب کن
# (pip install torch --index-url https://download.pytorch.org/whl/cpu) تا
# pip سراغ wheel غول‌پیکر CUDA نره — جزئیات در specs/spec4.md.
chromadb==0.5.23
sentence-transformers==3.3.1

# تست
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==6.0.0
fakeredis==2.37.0

# لینت/فرمت
ruff==0.16.2
